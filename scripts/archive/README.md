# Archived Scripts

Scripts in this directory are no longer actively used but are preserved for
reference. They were moved here during the v22.0 code quality remediation.

If you need to resurrect a script, move it back to `scripts/` and add a
`package.json` entry if it should be user-invocable.

## Contents

| Script                                | Reason Archived                |
| ------------------------------------- | ------------------------------ |
| `evidence-spotcheck-v17-5.py`         | v17.5-era one-off              |
| `generate-v17-5-ai-batches.py`        | v17.5-era one-off              |
| `governance-qa-v17-5-ai-ingestion.py` | v17.5-era one-off              |
| `normalize-v17-5-ai-outputs.py`       | v17.5-era one-off              |
| `validate-v17-5-ai-normalized.py`     | v17.5-era one-off              |
| `fix-all-impact-i18n.ts`              | One-off i18n fix               |
| `fix-impact-i18n.ts`                  | One-off i18n fix               |
| `fix-missing-service-fr.ts`           | One-off i18n fix               |
| `patch-i18n.ts`                       | One-off i18n fix               |
| `extract-missing-keys.ts`             | One-off i18n fix               |
| `check-vercel-usage.sh`               | Stale (VPS migration complete) |
| `copy-vercel-config.sh`               | Stale (VPS migration complete) |
| `vercel-ignore-build.sh`              | Stale (VPS migration complete) |
| `debug-rls.ts`                        | One-off debugging              |
| `deploy-vps-proof.sh`                 | One-time deployment proof      |
| `release-vps-proof.sh`                | One-time release proof         |
| `enable-plain-language.ts`            | One-off data migration         |
| `check-env-host.ts`                   | One-off environment check      |
